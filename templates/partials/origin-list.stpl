<% if !&origins.is_empty() {%>
    <div class="origins">
        <span role="list"><% for (i, origin) in origins.iter().enumerate() { %><wbr /><a><%= &origin.text %></a><wbr /><% if i != (&origins.len() - 1) { %>, <% } %><% } %></span>
    </div>
<% } %>