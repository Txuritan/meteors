{{ #for (i, origin) in self.origins.iter().enumerate() }}{{ Link::new(Contrast::Low, format!("/origin/{}", origin.id), origin.text.clone()).render(writer) }}{{ #if i != (self.origins.len() - 1) }}<span class="text-opacity-60 text-white">, </span>{{ /if }}{{ /for }}